const Dashboard = () => {
    return (
        <div className="container py-5">
            <h1>Dashboard</h1>
            <p className="lead">Nur sichtbar, wenn eingeloggt.</p>
        </div>
    );
};

export default Dashboard;